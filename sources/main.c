#include "minishell.h"

int main(void)
{
    t_shell	shell;

    /*
    init();
    while(exit_status or true)
    {
        readline();
        parser();
        execute();
    }
    */

    return (EXIT_SUCCESS);
}